# Performance

No performance measurement has run because a compiler and executable are unavailable. CPU model, RAM, compiler, Release build, and observed processing use therefore remain **NOT MEASURED**.

Required measurement matrix after a verified build: 44.1/48/96 kHz, 512 samples, analyzer enabled and disabled, with representative stereo programme material for at least 30 minutes. Record Windows build, CPU model, RAM, MSVC version, sample rate, buffer, analyzer state, mean/maximum processor CPU, glitches, and denormal/silence behavior. Do not compare Debug numbers with Release.

